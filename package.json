{
  "name": "prompt-perfect",
  "version": "1.0.0",
  "private": true,
  "description": "An AI-powered application to help users construct, refine, and optimize prompts for Large Language Models. It breaks down prompt creation into key components and uses AI to suggest improvements for better clarity, specificity, and structure.",
  "main": "index.js",
  "scripts": {
    "start": "echo \"Error: no start script specified, please use a local server to serve index.html\" && exit 1",
    "build": "echo \"Error: no build script specified\" && exit 1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jacob Werner",
  "license": "MIT",
  "dependencies": {
    "@google/genai": "^1.11.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "typescript": "^5.5.3"
  }
}
